# Sweet Drop Riches Project Spec

> 本文件用途：讓後續製作 Sweet Drop Riches 更穩定、更像博弈遊戲，而不是只做表面美術。  
> 若本文件、舊版本、舊截圖、舊說法和使用者最新指示衝突，一律以使用者最新指示為準。  
> 本文件不是完整程式規格。實作細節請讀 `script.js`、`styles.css`、`index.html`、`assets/` 與最新 commit。

## 1. 專案資料

- 本機路徑：`C:\Users\User\Desktop\Design`
- GitHub：https://github.com/alltake18-beep/Sweet-Drop-Riches
- GitHub Pages：https://alltake18-beep.github.io/Sweet-Drop-Riches/
- 主要檔案：
  - `PROJECT_SPEC.md`
  - `index.html`
  - `styles.css`
  - `script.js`
- 主要資產：
  - `assets/symbols/`
  - `assets/ui/`
  - `assets/ui/wins/`
- `.codex-remote-attachments/` 是未追蹤資料夾，不要提交。

## 2. 工作規則

- 製作前先列本次製作內容，等使用者確認。
- 使用者確認後才開始改。
- 製作中不要一直回報，不要講工具過程，不要廢話。
- 完成後只條列：
  - 改動項目
  - 驗證結果
  - 遊戲網址
- 若需要部署或同步，完成後要 push GitHub，並提供帶版本與時間參數的最新網址清快取。
- 遇到工具失敗時，先判斷是工具問題還是專案問題，不要一直重試同一個失敗工具。
- 目前不要用瀏覽器工具，除非使用者重新允許。
- 驗證優先使用：
  - 本機檔案檢查
  - `node --check script.js`
  - `git diff`
- 如果核心流程不穩，不要只補一個小 if；要檢查並重整整套流程。
- 不要只修截圖提到的單一物件。所有倍率、所有物件、所有消除來源、所有掉落補牌流程都要一起檢查。
- 若需要權限，理由要合併說明本次所有必要動作，不要每一步問一次。

## 3. 博弈製作優先序

Sweet Drop Riches 是手機直版 Slot / Casino 消除遊戲。所有美術、前端、音效、動效、數學、流程都要服務下面優先序。

1. 大獎目標  
   玩家要一眼知道自己在追高倍率、BIG WIN 系列字卡、爆分、滿槽獎勵。

2. 贏錢反饋  
   倍數糖落槽、加分、跑分、字卡、音效要讓玩家感覺真的贏到了。

3. 遊戲節奏  
   消除、掉落、火焰、事件、收集、結算要快而清楚，不能拖或亂。

4. Near Miss  
   差一格落槽、差一顆滿槽、差一點打中高倍率，都要讓玩家看得懂。

5. 跳檻能力  
   5x、10x、20x、30x、50x、100x、200x 的升級感要明顯。

6. 爆點密度  
   事件、火焰槍、倍數糖落槽、大獎字卡不能太稀，畫面要有期待感。

7. 玩法特色  
   1x1 / 2x2 倍數糖、火焰效果、事件收集槽是核心記憶點。

8. 視覺高潮  
   美術、動效、音效要強化大獎、爆分、收集、Near Miss，不只是裝飾。

## 4. 美術規範

- 玩家看得到的東西都要用正式美術資產規格製作，不要用 CSS 幾何圖形假裝美術。
- 重要物件完成圖都要先給使用者確認，沒過就重畫。
- 美術要先考慮手機 1x1 尺寸能不能辨識，再考慮大圖細節。
- 倍數糖是盤面最高視覺層級：
  - 要比一般糖更立體、更厚、更像獎品。
  - 可以爆框，但不能壓壞鄰格資訊。
  - 1x1 要清楚跳出來，2x2 要有大獎目標感。
  - X100 / X200 是高獎物件，要用最高規格處理。
- 倍數糖不應只靠文字辨識，造型、顏色、輪廓都要有層級。
- 倍數糖底圖和倍率文字要有明確安全區，避免文字偏移、裁切、被框壓住。
- 一般糖不能搶走倍數糖視覺重點。
- 一般綠色糖不要做成滿版方塊感，避免看起來比特殊物件更重。
- Logo、大獎字卡、倍數糖、事件圖示都要有商品級完成度。
- 所有資產要乾淨，避免髒邊、破圖、陰影蓋住、裁切殘留。

## 5. 前端規範

- 手機直版優先，畫面比例和觸控體驗以手機為主。
- UI 要像 Slot / Casino 操作台，不像一般網站或普通 Match-3。
- 首屏必須直接是遊戲，不要做 landing page。
- 盤面、事件槽、倍率槽、底部操作列要有穩定尺寸，不能因文字或特效跳動。
- 文字不能爆版、重疊、被裁切。
- 倍數糖 1x1 / 2x2 要分開考慮尺寸、字級、爆框、安全區。
- 事件預覽圖要置中，不能看起來像臨時縮圖。
- 不要增加多餘教學文字；能用畫面層級、圖示、動效讓玩家懂。
- 若調整 UI，必須同時看盤面、事件槽、底部槽位、按鈕區是否互相壓迫。

## 6. 動效規範

- 動效要快、清楚、有情緒，不要拖時間。
- 消除、掉落、補牌、事件、火焰、倍率落槽、大獎都要有不同強度。
- 跑動或輪播效果要能建立期待感，可以由快變慢。
- 火焰流程要讓玩家看懂：
  - 紅底停住。
  - 清一般糖。
  - 未破壞倍數糖留在原位並顯示抗火效果。
  - 停留一小段時間。
  - 再掉落補牌。
  - 未破壞倍數糖要有明確從原位置往下掉的表演。
- 倍數糖落槽要有吸入、爆點、槽框亮起，但不要環繞光，不要一直閃。
- 特效不能遮住核心資訊，不能讓玩家看不懂發生什麼事。
- 若掉落、收集、補牌出現不穩，優先檢查整套流程，不要只修單點。

## 7. 音效規範

- 背景音樂要中節奏、洗腦、Casino groove，進入遊戲就應該能聽到。
- BGM 音量要足夠，但不能蓋過消除、火焰、倍率落槽、大獎音效。
- 一般消除、Cascade、事件觸發、火焰、倍率落槽、大獎跑分要分層。
- 火焰消除要有火燒音效，不要和普通消除聽起來一樣。
- 倍數糖落到底部槽位時要有特殊音效。
- 高倍率、大獎字卡、爆分音效要更強；低倍率不要亂用 jackpot 感。
- 音效不能過度同時觸發，避免吵、糊、沒有重點。

## 8. 後端 / 資料規範

- 目前專案是前端原型。
- 若未來做正式博弈版本，不能讓前端自行決定賠付結果。
- 正式版需要考慮：
  - 結果可重播
  - 權重可設定
  - 版本可追蹤
  - 獎勵可稽核
  - 測試環境與正式環境分離
- 數學設定、事件結果、倍率結果、獎勵結果要能被記錄與追查。
- 前端可以負責表演，但正式賠付與結果來源應由可信邏輯決定。

## 9. 數學規範

- 權重、倍率、事件門檻要集中設定，方便調整與檢查。
- 不要宣稱 RTP、波動、命中率已完成，除非有模擬、表格與驗證。
- 每次調整機率，都要說明影響：
  - 爆點密度
  - 玩家期待感
  - 高倍率稀有度
  - 遊戲節奏
  - Near Miss 感受
- 高倍率要稀有但值得期待，不能只靠文字說是大獎。
- X100 / X200 屬於高獎目標，視覺、音效、動效、數學稀有度都要對齊。
- 若新增倍率或事件，要同步檢查：
  - 盤面生成
  - 事件收集槽
  - 權重設定
  - 大獎字卡
  - 美術資產
  - 音效與動效
  - 掉落與收集流程

## 10. 目前遊戲規則參考

以下只保留高層規則。若和程式或最新指示衝突，以最新指示與目前實作為準。

- 手機直版 Slot / Casino 消除遊戲。
- 盤面 6x9。
- 一般物件 5 種。
- 一般消除不直接計分，主要推進事件收集槽。
- 事件收集門檻目前以最新實作為準。
- 倍數糖由專門狀態管理，不要當一般 1x1 糖塞回 board。
- 倍數糖可有 1x1 與 2x2，X100 / X200 目前以 2x2 高獎物件處理。
- 倍數糖落到底部槽位才收集。
- 玩家交換 1x1 倍數糖時，必須是交換後一般物件可以形成有效消除才成立。
- 火焰效果、一般物件事件、玩家消除、Cascade 都應接回完整掉落補牌流程。

## 11. 舊規格清理

以下舊內容若在舊文件、舊截圖、舊版本中出現，不再當成目前規格：

- 舊巧克力糖收集規則。
- 舊 sniper / 狙擊槍說法。
- 舊 20 收集門檻。
- 舊 6 個底部收集槽。
- 舊倍數糖合併規則。
- 舊倍數糖只靠系統文字或 CSS 造型的方向。
- 舊火焰或事件流程若和目前穩定流程衝突，必須重新檢查整套流程。

## 12. 驗收方式

- 文件調整：
  - 檢查 `PROJECT_SPEC.md`
  - 檢查 `git diff PROJECT_SPEC.md`
- 程式調整：
  - `node --check script.js`
  - `git diff`
- 視覺 / UI 調整：
  - 優先用手機直版檢查。
  - 看 1x1、2x2、事件預覽、底部槽位、文字安全區。
  - 目前不要用瀏覽器工具，除非使用者重新允許。
- 部署：
  - 需要同步時 push GitHub。
  - 提供帶版本與時間參數的 GitHub Pages 網址清快取。
