# Sweet Drop Riches Project Spec

> 本文件用途：讓後續在 `C:\Users\User\Desktop\Design` 開的新聊天室，都能用同一套製作基準繼續 Sweet Drop Riches。  
> 本文件不是最高指令。若本文件、舊聊天、舊截圖、舊 commit 與使用者最新指示衝突，一律以使用者最新指示為準。  
> 實作細節永遠以最新 `index.html`、`styles.css`、`script.js`、`assets/` 與 Git commit 為準。

## 1. 專案資料

- 本機路徑：`C:\Users\User\Desktop\Design`
- GitHub：https://github.com/alltake18-beep/Sweet-Drop-Riches
- GitHub Pages：https://alltake18-beep.github.io/Sweet-Drop-Riches/
- 主要檔案：
  - `PROJECT_SPEC.md`
  - `index.html`
  - `styles.css`
  - `script.js`
- 主要資產：
  - `assets/symbols/`
  - `assets/ui/`
  - `assets/ui/wins/`
- `.codex-remote-attachments/` 不要提交。

## 2. 合作與工作規則

- 製作前先條列本次改動內容，等使用者確認。
- 使用者確認後才開始改檔。
- 大範圍調整要分段做：每段改完先檢查，確認沒問題再繼續下一段。
- 不確定機制時先讀碼、搜尋、驗證，不要猜。
- 如果改 A 時發現相關問題，要一併列出或一起修，不要只修表面。
- 工具失敗時先判斷原因，不要一直重試同一個失敗工具。
- 過程不要一直回報，不要廢話；必要更新要短、具體。
- 完成後只條列：
  - 改動項目
  - 驗證結果
  - 遊戲網址，僅限已 push 後提供
- 尚未 push 前不要提供遊戲網址。
- 使用者要求 Push 時：
  - 更新 CSS/JS cache-bust。
  - commit 並 push GitHub。
  - 提供帶 commit 與時間參數的手機強刷網址，例如：`?v=<commit>&t=<yyyyMMdd-HHmmss>`。
- 目前不要用瀏覽器工具，除非使用者重新允許。
- 驗證優先使用：
  - 本機檔案檢查
  - `node --check script.js`
  - `git diff`
  - `git status --short --untracked-files=no`

## 3. 製作態度

- 使用者重視「一次檢查完整」，不是只修截圖上的單點。
- UI 問題要檢查關聯層：DOM、CSS final override、舊樣式殘留、cache-bust、手機顯示。
- 機率、倍率、派彩、BET、收集槽、事件槽、音效都可能互相影響，不要孤立處理。
- 若要改數學或 RTP 相關內容，要列出精確分佈與影響來源。
- 若使用者表示某次處理方式滿意，後續應沿用：先拆解、逐段實作、逐段驗證、簡短回報。

## 4. 目前遊戲方向

- Sweet Drop Riches 是手機直版 Slot / Casino 風格消除遊戲。
- 首屏就是遊戲，不做 landing page。
- 畫面要像 casino cabinet hardware，不像一般 mobile HUD。
- 盤面是 6x9。
- 一般物件 5 種。
- 一般消除不直接給錢，主要推進上方事件能量條。
- 上方事件能量條門檻目前以最新程式為準，視覺上有 `9 / 21 / 40`。
- 下方有三個倍數收集槽。
- 倍數糖落到底部倍數槽才收集。
- 三個下方倍數槽收滿後，進入中央轉盤表演並發放轉盤獎勵。
- 火焰、糖果消除事件、倍數糖生成、轉盤結果，都要接回完整掉落補牌流程，不能破壞盤面穩定。

## 5. BET 與倍數糖分數規則

- 玩家應該能隨時調整 BET。
- 倍數糖生成時，要用當下 BET 計算並鎖定實際可得分數。
- 之後玩家切換 BET，不會改變已存在倍數糖的分數。
- 新出現的倍數糖才使用新的 BET。
- 盤面倍數糖、事件預覽、底部收集槽，都顯示玩家實際可拿到的分數，不只顯示 `x倍率`。
- 內部仍可保留倍率值，用於尺寸、稀有度、音效、火焰判斷。
- Bet `1000` 時也不能爆框、不能裁字；分數可以佔滿格子，但要完整可讀。
- Balance 顯示小數後兩位。
- 目前初始倍數糖參數：
  - 初始數量：`2` 顆 `65%`，`3` 顆 `35%`
  - `1x1`: `94%`
  - `2x2`: `6%`
  - `1x1` 倍率：`x5 76%`、`x10 20%`、`x20 4%`
  - `2x2` 倍率：`x30 97.5%`、`x50 1.8%`、`x100 0.45%`、`x200 0.25%`
- 倍數糖生成要使用 Reel 分配：
  - Reel 1 = `col 0-1`
  - Reel 2 = `col 2-3`
  - Reel 3 = `col 4-5`
  - 生成倍數糖時，優先選沒有盤面倍數糖、也沒有底部已收集值的 Reel。
  - 如果三個 Reel 都不可用，才 fallback 隨機。
  - `2x2` 倍數糖固定在 Reel 起始欄 `0 / 2 / 4`。
  - `1x1` 倍數糖放在該 Reel 內任一欄。

## 6. 下方三槽轉盤規則

- 三個下方倍數槽收滿後，畫面中央出現大型轉盤。
- 三個槽內分數要飛到轉盤中心圓圈。
- 表演要展示三個槽位分數相加。
- 轉盤開始轉動後到結束總長約 5 秒。
- 轉動要由快到慢，不要突然停下。
- 轉動時要有轉盤音效。
- 轉盤樣式要符合糖果 casino 主題，不要偏離機台美術。
- 轉盤獎項文字要大、清楚、內移，不可貼邊；要有足夠描邊、陰影與外光暈，讓 `0.1x` 到 `100x` 都能讀。
- 轉盤區域依獎項數量平均切分，但實際結果由機率決定。
- 目前轉盤獎項機率：
  - `0.1x`: 26%
  - `0.2x`: 26%
  - `0.5x`: 31%
  - `1x`: 9%
  - `1.5x`: 3.5%
  - `2x`: 2.2%
  - `5x`: 1%
  - `10x`: 0.45%
  - `20x`: 0.25%
  - `30x`: 0.1%
  - `50x`: 0.05%
  - `100x`: 0%
- 轉盤獎勵 = 三槽分數總和乘上轉盤倍率。
- 發獎後清空三個倍數槽，但保留原有盤面；不要刷新全新盤面。
- 目前底部倍數槽保留時間：`SLOT_TURN_MAX = 20`。

## 7. 收集槽事件與火焰事件

- 目前能量條門檻：`9 / 21 / 40`。
- 第三槽從 `33` 拉到 `40`，目標是讓第三槽成為明顯跳檻，降低第三槽平均觸發次數，但保留較強記憶點。
- 第 1 / 2 槽事件機率：
  - 紅/藍/綠/橘/紫糖消除：各 `5%`
  - `x5`: `20%`
  - `x10`: `13.2%`
  - `x20`: `2.4%`
  - `x30`: `1%`
  - `x50`: `0.25%`
  - `x100`: `0.08%`
  - `x200`: `0.02%`
  - 火焰：`37.45%`
- 第 3 槽事件機率：
  - `x20`: `27%`
  - `x30`: `22%`
  - `x50`: `7%`
  - `x100`: `1.5%`
  - `x200`: `0.5%`
  - 火焰：`42%`
- 收集槽觸發 5 種糖果消除時，5 種糖果可共用同一個事件消除音效。
- 火焰展示消除位置時要有預警或掃描音效。
- 火焰實際消除時要有燃燒/命中音效。
- 火焰流程要讓玩家看懂：
  - 先展示位置
  - 再進入最終命中
  - 再消除或抵抗
- 火焰是 RTP 控制來源之一，火焰機率與破壞機率調整時要明確列出。
- 目前火焰破壞機率：
  - 基礎：`0.55`
  - `x20`: `0.4`
  - `x50`: `0.28`
  - `x100+`: `0.18`
  - 第三槽火焰：以上機率 `x2`，上限 `1`

## 8. 音效規範

- 音效要手機聽得見，不要過度保守。
- 不能只調單一音效音量；要看整體 audio gain staging。
- 目前音效架構應保留：
  - Master Audio Bus
  - SFX group
  - BGM group
  - category gain
  - cooldown / max voices / attenuation
  - limiter
  - 80Hz 以下低頻削弱，避免手機喇叭破音
- SFX、BGM、按鈕、消除、倍數、爆分、火焰、轉盤要分層控制。
- BGM 要有糖果歡樂感。
- 當倍數糖收集接近特殊事件時，BGM 可以進入高潮階段，但不能蓋過 SFX。
- 同一音效不可在極短時間內無限制重複播放。
- 多音效疊加時不能 clipping、爆音、破音。
- 如果使用者說手機聽不見，優先檢查 master / group / peak limit / category gain，而不是只加大單顆音效。

## 9. 美術與 UI 規範

- 手機直版優先。
- UI 要像 Slot / Casino 操作台，不像一般網站或普通 Match-3。
- 底部 Balance / Bet / Turbo / Menu 必須整合進機台底座美術槽。
- 底部四個美術槽本身就是 layout containers：
  - Slot 1 = Balance
  - Slot 2 = Bet
  - Slot 3 = Turbo
  - Slot 4 = Menu
- 不要用螢幕座標硬放底部 UI。
- 不要新增前端面板破壞底座。
- 不要把控制做成像 mobile HUD。
- 盤面、事件槽、倍率槽、底部操作列要有穩定尺寸，不能因文字或特效跳動。
- 文字不能爆版、重疊、被裁切。
- 底部三個倍數槽內的分數要水平置中並偏下，不要貼在火焰或上緣。
- 事件預覽圖要置中，不能看起來像臨時縮圖。
- 倍數糖是盤面最高視覺層級，造型、顏色、輪廓、文字安全區都要清楚。
- 重要美術或 UI 大改，應先產審核圖或清楚說明方案，確認後再正式套檔。

## 10. 動效規範

- 動效要快、清楚、有情緒，不要拖時間。
- 輪播效果要建立期待感，可以由快變慢。
- 輪播時不要無限上下瘋狂抖動；每次圖案切換時抖動一次即可。
- 消除、掉落、補牌、事件、火焰、倍率落槽、大獎、轉盤都要有不同強度。
- 特效不能遮住核心資訊，不能讓玩家看不懂發生什麼事。
- 物件消除收集特效要飛到上方 `9 / 21 / 40` 的能量條內，不要看起來飛到盤面或能量條外。
- 倍數糖落槽是否有粒子，要依最新指示；目前倍數糖收集不需要額外粒子時，不要擅自加回。

## 11. 數學與 RTP 規範

- 不要宣稱 RTP、波動、命中率已完成，除非有模擬、表格與驗證。
- 倍數糖、初始盤面、事件槽、第三階段事件、火焰破壞率、轉盤、下方槽滿表演，都會影響 RTP。
- 調整機率時要列出精確分佈。
- 高倍率要稀有但值得期待。
- `x100` / `x200` 是高獎目標，視覺、音效、動效、數學稀有度要一致。
- 若使用者說 RTP 太高，優先檢查：
  - 初始盤面倍數糖數量
  - 初始倍數糖值分佈
  - 第三階段事件機率
  - 高倍率出現率
  - 火焰破壞率
  - 三槽轉盤期望值
  - 是否有額外 bonus 派彩

### RTP 輕量模擬工具

- 本機工具：`tools/rtp-sim.js`。
- 預設指令：
  - `node tools/rtp-sim.js`
- 可自訂：
  - `node tools/rtp-sim.js --steps 1000 --bet 100 --start-balance 4000 --cashout-balance 10000`
  - 指定報告位置：`node tools/rtp-sim.js --out reports/rtp-summary.md`
  - 需要可重現結果時可加 `--seed 任意文字`。
- 跑完後預設會輸出本機報告檔：`reports/rtp-summary.md`。
- 回覆使用者時，要提供可點開的本機報告檔連結。
- 預設口徑：
  - 總步數：`1000`
  - 每位玩家初始資產：`4000`
  - Bet：`100`
  - 玩家單人移動步數不限制。
  - 玩家資產不足 `100` 時離場，刷新下一位玩家與盤面。
  - 玩家資產大於 `10000` 時離場，刷新下一位玩家與盤面。
  - 總步數跑完時停止，所以總玩家人數不是固定值。
  - 移動策略：每次選當下預估分最高的合法移動，模擬偏聰明玩家。
- 退幣率公式：
  - `資產大於 10000 的玩家人數 / 總玩家人數`
- 模擬輸出只保留摘要，不印每一步，避免聊天上下文被大量消耗。
- 輸出欄位要包含：
  - 總步數
  - 實際完成步數
  - 總玩家人數
  - 初始資產
  - 押注
  - 總押注
  - 總贏分
  - RTP
  - 退幣率
  - 資產大於 `10000` 離場人數
  - 資產不足 `100` 離場人數
  - 未結束玩家人數
  - 倍數糖 RTP：顯示倍數糖總 RTP，標題格式為 `倍數糖RTP：xx.xx%`
  - 收集槽觸發統計：第一槽、第二槽、第三槽平均幾轉出現
  - 轉輪觸發 RTP：顯示轉輪總 RTP，標題格式為 `轉輪觸發RTP：xx.xx%`
  - 轉輪觸發統計要顯示 `平均幾轉觸發`，計算方式為 `實際完成步數 / 轉輪觸發次數`。
  - `平均幾轉觸發` 下方要顯示 `實際拿到的平均倍數：X.XXXx（轉輪平均乘倍 Y.YYYx）`。
  - `實際拿到的平均倍數` 計算方式為 `轉輪總贏分 / 轉輪觸發次數 / Bet`。
  - `轉輪平均乘倍` 計算方式為 `轉輪總贏分 / 三槽總底分`。
  - 倍數糖明細要用 Markdown 表格：`倍數糖 / 次數 / 贏分 / RTP`。
  - 收集槽觸發統計要用 Markdown 表格：`收集槽 / 平均幾轉出現 / 次數`。
  - 轉輪明細要用 Markdown 表格：`結果 / 次數 / 佔總RTP`；不要在每個固定獎項列重複顯示 `平均倍數 0.100x` 這類沒有意義的欄位。
- 目前正式遊戲 `scoreRuns()` 回傳 `0`，所以輕量模擬的主要贏分來源是倍數糖收集與三槽滿轉輪。
- 輕量模擬用途是幫助比較機率調整方向，不等於正式後端 RTP 認證。
- 若正式遊戲核心邏輯變動，尤其是倍數糖掉落、火焰破壞率、事件槽機率、轉輪機率或一般消除計分，必須同步更新 `tools/rtp-sim.js`。

## 12. 後端 / 正式版提醒

- 目前專案是前端原型。
- 若未來做正式博弈版本，不能讓前端自行決定正式賠付結果。
- 正式版需要可追查：
  - 下注
  - 事件結果
  - 倍數糖生成
  - 轉盤結果
  - 派彩結果
  - 隨機來源
- 前端可以負責表演，但正式賠付與結果來源應由可信邏輯決定。

## 13. 舊資料清理原則

- 不要因舊文件或舊聊天把已淘汰方向加回來。
- 以下若沒有使用者重新指定，不要恢復：
  - 舊巧克力糖收集規則
  - 舊 6 個底部收集槽
  - 舊倍數糖合併規則
  - 舊倍數糖只顯示 `x倍率` 的 UI
  - 舊的純 CSS 假美術方向
  - 舊的下方三槽滿直接給固定 `bet * 10` bonus
- 如果舊規格與目前穩定流程衝突，先讀最新程式並向使用者列出差異。

## 14. 驗收方式

- 文件調整：
  - 讀 `PROJECT_SPEC.md`
  - 檢查 `git diff PROJECT_SPEC.md`
- 程式調整：
  - `node --check script.js`
  - `git diff`
  - 搜尋是否有舊邏輯殘留
- 視覺 / UI 調整：
  - 檢查手機尺寸下文字是否爆框、重疊、被裁切
  - 檢查底部 UI 是否仍在機台槽內
  - 檢查盤面、事件槽、倍數槽、轉盤是否互相遮擋
- 部署：
  - 使用者要求 Push 才 push
  - Push 後提供帶 commit 與時間參數的 GitHub Pages 強刷網址

## Push / Mobile Cache-Bust Rule

- Every time the user asks to push, refresh the mobile cache hash before committing.
- Update both asset URLs in `index.html`:
  - `styles.css?v=<topic-or-short-commit>-<yyyyMMdd-HHmmss>`
  - `script.js?v=<topic-or-short-commit>-<yyyyMMdd-HHmmss>`
- After pushing, provide a GitHub Pages URL with a page-level cache-bust query:
  - `https://alltake18-beep.github.io/Sweet-Drop-Riches/?v=<commit>&t=<yyyyMMdd-HHmmss>`
- Do not provide a game URL before the push succeeds.
- Do not commit `.codex-remote-attachments/`.
